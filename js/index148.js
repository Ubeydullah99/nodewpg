const superhero = {
    name: 'Batman',
    secretName: 'Bruce',
    sidekick: ['Robin', 'Alfred', 'Gordon'],
    strength: 70,
};
console.log(`superheros's name: ${superhero.name}`);
console.log(`superheros's secret name: ${superhero.secretName}`);
console.log(`superheros's side kick: ${superhero.sidekick}`);
console.log(`superheros's strength:${superhero.strength}`);

superhero.speed = 90;
console.log(`superheros's speed:${superhero.speed}`);
