const text='HELLO';
let result=text.charAt(4);
result+=text.charAt(3);
result+=text.charAt(2);
result+=text.charAt(1);
console.log(result);
const message=`The final result is: ${result}`;
console.log(message.toLowerCase());
